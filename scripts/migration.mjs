// move ITEM TYPES from required to filter.
// move THROW TYPES from required to filter.
// move ITEM REQUIREMENTS from required to filter.
// convert filters.arbitraryComparison into an array of [{one,operator,other}].
// convert identifier to id and create new ids (16 characters, regular foundry ids)? (stupid)
// convert 'label' to 'name'.
// rework 'bonuses.<type>.<identifier>.{...values}' to 'bonuses.<id>.{...values}'.
// rename 'values' to 'bonuses'.
